import 'package:flutter/material.dart';

class DashboardLiveWidget extends StatelessWidget {
  const DashboardLiveWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
